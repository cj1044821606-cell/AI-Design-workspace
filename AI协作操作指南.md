# AI 协作快速上手

这份文档只保留最短路径。完整规则以 `AGENTS.md` 为准。

## 1. 先做什么

新接手的 AI，默认先读：

1. `AGENTS.md`
2. `ARCHITECTURE.md`
3. 当前任务对应的基准模板

## 2. 先判断哪种任务

- `画册`
- `datasheet`
- `折页/宣传册`

再判断你改的是：

- `内容`
- `排版`
- `版本顺序`
- `产品图位置`

## 3. 画册任务的最短流程

1. 确认页面类型：封面、目录、章节页、标准产品页、PV 产品页
2. 确认是旧结构还是新结构
3. 找基准模板或共享渲染壳
4. 从素材库调用真实素材
5. 修改内容或主数据
6. 同步目录、装配、小地图
7. 保留 `ProductImageEditor` 和 transform 注释
8. 预览并检查出界、重叠、顺序

## 4. 最常见的错误

- 把页面当普通网页写
- 新增产品页时自创布局
- 漏改目录
- 覆盖 `initialX / initialY / initialScale`
- 用删除源文件代替版本过滤
- 长文本出界时先乱动模块起点

## 5. 一句提醒

这个系统里，AI 不是自由设计师，而是“在固定模板和固定版本关系里工作的排版执行者”。
