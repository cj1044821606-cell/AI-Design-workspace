# Datasheet 模板专项规则

这份文档只补充 datasheet 的专项要求。通用规则仍看 `AGENTS.md`。

## 1. 适用范围

适用于：

- 电池 datasheet
- 逆变器 datasheet
- 单页参数物料

不适用于：

- 画册跨页
- 章节页
- 目录页

## 2. 基本原则

- 优先继承现有 datasheet 基准模板
- 不自创全新布局，除非用户明确要求新模板
- Logo、背景、红色标题条、表格系统尽量保留既有结构
- 产品图必须优先使用 `ProductImageEditor`

## 3. 产品图规则

必须保留：

- `ProductImageEditor`
- `componentName`
- `initialX`
- `initialY`
- `initialScale`
- 对应的 transform 注释

不要：

- 用普通 `<img>` 代替
- 删除 transform 标记
- 因为想省事就把产品图位置写死

## 4. 标题与卖点规则

- 标题位置保持模板基准
- 红色标题条可以跟随文字自适应长度
- 卖点区保留现有层级关系
- 图标要按卖点语义重新选，不要机械复用旧图标

## 5. 参数表规则

- 优先保留现有配色和结构
- 行高、边框、表头样式与模板保持一致
- 当单元格内容很多时，优先增加上下内边距或允许换行
- 不要为了塞下长文本随意缩小整张表的字号

## 6. 导出注意事项

- 避免 `drop-shadow`、`filter`、大面积透明
- 尽量保证字体走项目已有字体系统
- 输出目标是稳定导出 PDF，并尽可能方便后续进入 Adobe Illustrator

## 7. 给 AI 的建议

改 datasheet 时，先把它理解成“固定信息结构的参数单页”，而不是“自由海报”。优先保证信息清晰、参数准确、版式一致，再考虑视觉微调。
