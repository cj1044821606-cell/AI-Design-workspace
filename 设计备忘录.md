# 品牌设计备忘录 (Brand Design Memo)

## 1. 企业背景
- **所属行业**：能源科技（储能设备）

## 2. 品牌色彩规范
- **主品牌色**：CMYK (0, 100, 70, 0) 
  *(注：转换为网页/UI使用的 HEX 色值约为 `#E60039` 或 `#FF004D` 亮红色，具体以VI标准为准)*
- **背景色**：尽量以**白色**为底色（除非有特殊要求），以保证画面的干净与通透。

## 3. 视觉风格与排版原则
- **画面调性**：整体偏明亮、通透。
- **排版风格**：简约、大方、有质感，偏向**线下实体印刷物料**风格（避免过度使用网页感的投影、毛玻璃等特效）。
- **Logo规范**：封面 Logo **严格固定在左上角**（物理尺寸距顶边约 6.5mm，距左边约 9mm；在预览代码中对应绝对定位 `top-6 left-8`）。无论版式如何变化，Logo 位置必须保持一致，以建立统一的品牌视觉锚点。
- **设计建议**：
  - 保持充足的留白（Whitespace），避免元素过度拥挤，提升“呼吸感”与“高级感”。
  - 字体层级分明，利用已上传的 MarkOT 系列字体（Medium, Regular, Italic等）建立清晰的信息结构。
  - 品牌色（CMYK: 0,100,70,0）作为点缀色或强调色使用，在白色底色上形成强烈的视觉焦点，体现科技与能源的活力。

## 4. 品牌素材库状态
- **位置**：`/品牌素材库/`
- **现有资产**：
  - 品牌VI手册 (`itel energy 品牌VI2.pdf`)
  - 品牌专属字体 (`MarkOT` 系列字体文件)

## 5. 画册产品页规范 (Catalog Product Pages)
- **参数表格 (Parameter Table)**：
  - **尺寸与位置**：表格左边缘距离画布左边缘 `248.151mm`，右边缘距离画布右边缘 `21.75mm`（表格宽度约为 `150.099mm`）。
  - **行高**：标准数据行高固定为 `4.551mm`。
  - **多行文本**：对于内容较多、需要换行的单元格，不适用固定行高，改为使用 `py-[2mm]`（文本离单元格上下边缘 2mm）。
  - **边框**：所有单元格之间由 `0.368pt` 粗细的纯白线（`#ffffff`）相隔。
  - **颜色规范**：
    - 大标题（如 Model 行）：背景色 `#9e9e9e`，文字白色加粗。
    - 副标题（分类行）：背景色 `#cac9c8`，文字 `#231A16` 加粗。
    - 正文（数据行）：背景色 `#dbdcdb`，文字 `#231A16`。
- **产品图交互 (Product Image)**：
  - 画册中出现的产品图必须使用 `ProductImageEditor` 组件。
  - 必须包含 `initialX`, `initialY`, `initialScale` 属性，并附带 `/* IMAGE_TRANSFORM_X */` 等注释，以支持空间调整并在点击确认后永久保存修改。
- **元素组命名 (Design Group Labels)**：
  - 页面上的所有逻辑元素组必须添加 `design-group-wrapper` 和 `design-group-label` 标签，以便通过 UI 上的小眼睛 icon 控制显示与隐藏。
